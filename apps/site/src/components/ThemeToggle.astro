<div class="chartkit-theme-switch" role="group" aria-label="Color theme">
  <button type="button" data-theme-value="dark" aria-pressed="true">
    Dark
  </button>
  <button type="button" data-theme-value="light" aria-pressed="false">
    Light
  </button>
</div>

<script>
  import "../theme-switch";
</script>

<style is:global>
  .chartkit-theme-switch {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2px;
    padding: 2px;
    border: 1px solid var(--hairline);
    border-radius: 8px;
    background: var(--surface-1);
  }

  .chartkit-theme-switch button {
    min-width: 48px;
    min-height: 30px;
    padding: 0 9px;
    border: 0;
    border-radius: 6px;
    background: transparent;
    color: var(--subtle);
    cursor: pointer;
    font: inherit;
    font-size: 0.72rem;
    font-weight: 500;
  }

  .chartkit-theme-switch button[aria-pressed="true"] {
    background: var(--surface-3);
    color: var(--ink);
    box-shadow: 0 0 0 1px var(--hairline) inset;
  }

  @media (max-width: 640px) {
    .chartkit-theme-switch {
      grid-template-columns: 1fr;
    }

    .chartkit-theme-switch button {
      min-width: 38px;
      min-height: 24px;
      font-size: 0.64rem;
    }
  }
</style>
