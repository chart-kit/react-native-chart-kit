# Native Performance Benchmark

Deprecated as an active matrix.

Use `npm run benchmark` for automated regression coverage. For Developer Preview, do not require native performance matrix completion.

For stable RC, collect concise performance evidence only where it changes release risk: device, surface, scenario, result, and notes. Avoid row-by-row matrix work unless a specific performance issue requires it.
